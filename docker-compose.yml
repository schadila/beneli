version: '2'
services:

  mysql:
    image: mysql:latest
    ports:
      - "33366:3306"
    volumes:
      - "/var/lib/mysql"
      - "./.docker/sql:/docker-entrypoint-initdb.d"
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: rezept-promo
      MYSQL_USER: rezept-promo
      MYSQL_PASSWORD: rezept-promo

  apache:
    image: chialab/php:5.6-apache
    ports:
      - "10000:80"
    volumes:
      - "./build:/var/www/html/"
    restart: always
    links:
      - mysql
    environment:
      DB_HOST: mysql
      DB_DATABASE: rezept-promo
      DB_USER: rezept-promo
      DB_PASSWORD: rezept-promo